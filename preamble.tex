	\documentclass[14pt,serif]{beamer}

% Encodage
	\usepackage[utf8]{inputenc}
	\usepackage[frenchb]{babel}
	\usepackage[T1]{fontenc}
	\frenchbsetup{StandardLists=true}

% Apparence
	\usetheme{Warsaw}
	\usepackage{charter}
	\usepackage{xcolor}
	\definecolor{main}{HTML}{355FB2}
	\setbeamercolor{structure}{fg=main}
	\setbeamertemplate{itemize item}[circle]
	\usepackage{upgreek}

% Commandes
	\newcommand{\gr}[1]{\textbf{\textcolor{main}{#1}}}
	\newcommand{\bloc}[2]{\begin{block}{#1}{#2}\end{block}}
